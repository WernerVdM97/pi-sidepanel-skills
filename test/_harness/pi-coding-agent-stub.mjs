/**
 * Test stub for @earendil-works/pi-coding-agent. Extensions only use
 * type-only imports from this package (erased by type stripping), so an
 * empty module suffices — present for safety should a value import ever
 * be added. Canonical copy lives in pi-sidepanel/test/_harness/.
 */
export {};
